@* 
Language Learning Challenge Component
Context: Educational language practice for 12-year-old players learning from owned territories
Educational Objective: Language pronunciation practice connected to geography and territory ownership
Safety Requirements: Age-appropriate vocabulary, positive encouragement, child-friendly design
*@
@using WorldLeaders.Shared.DTOs
@using WorldLeaders.Shared.Services
@using Microsoft.JSInterop
@inject ITerritoryService TerritoryService
@inject ISpeechRecognitionService SpeechRecognitionService
@inject IJSRuntime JSRuntime
@inject ILogger<LanguageLearningChallenge> Logger
@implements IAsyncDisposable

<div class="language-challenge-container">
    @if (isVisible)
    {
        <div class="modal-overlay" @onclick="CloseChallenge">
            <div class="modal-content" @onclick:stopPropagation="true">
                <div class="modal-header">
                    <h2 class="text-2xl font-bold text-purple-800">🗣️ Language Learning Adventure</h2>
                    <button @onclick="CloseChallenge" class="close-button">❌</button>
                </div>

                <div class="modal-body">
                    @if (isLoading)
                    {
                        <div class="text-center py-8">
                            <div class="inline-block animate-spin rounded-full h-8 w-8 border-b-2 border-purple-600"></div>
                            <p class="mt-2 text-purple-600">Loading language challenges...</p>
                        </div>
                    }
                    else if (challenges.Any())
                    {
                        @if (currentChallenge != null)
                        {
                            <!-- Current Challenge Display -->
                            <div class="challenge-display">
                                <div class="challenge-header">
                                    <div class="challenge-info">
                                        <h3 class="text-xl font-bold text-purple-800">
                                            Challenge @(currentChallengeIndex + 1) of @challenges.Count
                                        </h3>
                                        <p class="text-purple-600">Learn to say "Hello" in @currentChallenge.LanguageName!</p>
                                    </div>
                                    <div class="challenge-score">
                                        <div class="score-circle">
                                            <span class="text-2xl font-bold text-green-600">@correctAnswers</span>
                                            <span class="text-sm text-gray-600">/@challenges.Count</span>
                                        </div>
                                    </div>
                                </div>

                                <!-- Word to Learn -->
                                <div class="word-display">
                                    <div class="word-card">
                                        <h4 class="text-3xl font-bold text-blue-800 mb-2">@currentChallenge.Word</h4>
                                        <p class="text-lg text-blue-600 mb-4">Pronunciation: @currentChallenge.Pronunciation</p>
                                        
                                        <!-- Audio Playback -->
                                        <div class="audio-section mb-4">
                                            <button @onclick="PlayAudio" class="btn-child-friendly bg-blue-500 mb-2">
                                                🔊 Listen to Pronunciation
                                            </button>
                                        </div>

                                        <!-- Practice Input -->
                                        <div class="practice-section">
                                            <label class="block text-purple-700 font-semibold mb-2">
                                                🎤 Try to say it yourself! 
                                            </label>
                                            
                                            <!-- Speech Recognition Toggle -->
                                            <div class="speech-options mb-3">
                                                @if (speechRecognitionSupported && currentChallenge.SupportsSpeechRecognition)
                                                {
                                                    <div class="speech-mode-selector">
                                                        <button @onclick="() => SetInputMode(true)" 
                                                                class="btn-mode @(useSpeechInput ? "active" : "")">
                                                            🎤 Speak
                                                        </button>
                                                        <button @onclick="() => SetInputMode(false)" 
                                                                class="btn-mode @(!useSpeechInput ? "active" : "")">
                                                            ⌨️ Type
                                                        </button>
                                                    </div>
                                                }
                                                else
                                                {
                                                    <div class="speech-unavailable">
                                                        <span class="text-orange-600">🔄 Speech recognition setting up... You can type for now!</span>
                                                    </div>
                                                }
                                            </div>

                                            @if (useSpeechInput && speechRecognitionSupported)
                                            {
                                                <!-- Speech Recording Section -->
                                                <div class="speech-recording">
                                                    <div class="recording-controls">
                                                        @if (!isRecording)
                                                        {
                                                            <button @onclick="StartRecording" 
                                                                    class="btn-child-friendly bg-red-500 record-button"
                                                                    disabled="@isSubmitting">
                                                                🎤 Hold to Record
                                                            </button>
                                                        }
                                                        else
                                                        {
                                                            <button @onclick="StopRecording" 
                                                                    class="btn-child-friendly bg-gray-500 record-button recording">
                                                                🔴 Release to Stop
                                                            </button>
                                                        }
                                                    </div>
                                                    
                                                    @if (hasRecordedAudio)
                                                    {
                                                        <div class="recording-status">
                                                            <span class="text-green-600">✅ Audio recorded! Click "Check My Pronunciation" below.</span>
                                                        </div>
                                                    }
                                                    
                                                    @if (isRecording)
                                                    {
                                                        <div class="recording-indicator">
                                                            <div class="pulse-animation"></div>
                                                            <span class="text-red-600">🔴 Recording... speak clearly!</span>
                                                        </div>
                                                    }
                                                </div>
                                            }
                                            else
                                            {
                                                <!-- Text Input Section -->
                                                <input @bind="userInput" 
                                                       @onkeypress="HandleKeyPress"
                                                       class="practice-input" 
                                                       placeholder="Type what you heard or your pronunciation attempt..."
                                                       disabled="@isSubmitting">
                                            }
                                            
                                            <div class="input-buttons mt-3">
                                                <button @onclick="CheckAnswer" 
                                                        class="btn-child-friendly bg-green-500"
                                                        disabled="@((!hasRecordedAudio && useSpeechInput) || (string.IsNullOrWhiteSpace(userInput) && !useSpeechInput) || isSubmitting)">
                                                    @if (isSubmitting)
                                                    {
                                                        <span>🔄 Checking...</span>
                                                    }
                                                    else if (useSpeechInput)
                                                    {
                                                        <span>🎤 Check My Pronunciation</span>
                                                    }
                                                    else
                                                    {
                                                        <span>✅ Check My Answer</span>
                                                    }
                                                </button>
                                                
                                                <button @onclick="SkipChallenge" 
                                                        class="btn-child-friendly bg-gray-500 ml-2">
                                                    ⏭️ Skip This One
                                                </button>
                                            </div>
                                        </div>

                                        <!-- Feedback Display -->
                                        @if (!string.IsNullOrEmpty(feedbackMessage))
                                        {
                                            <div class="feedback-section mt-4">
                                                <div class="feedback-card @(isCorrect ? "correct" : "incorrect")">
                                                    <div class="feedback-icon">
                                                        @(isCorrect ? "🎉" : "💪")
                                                    </div>
                                                    <div class="feedback-content">
                                                        <p class="feedback-message">@feedbackMessage</p>
                                                        @if (!isCorrect)
                                                        {
                                                            <p class="feedback-hint">
                                                                💡 Hint: Listen carefully and try to match the sounds!
                                                            </p>
                                                        }
                                                    </div>
                                                </div>
                                            </div>
                                        }

                                        <!-- Progress Bar -->
                                        <div class="progress-section mt-6">
                                            <div class="progress-bar">
                                                <div class="progress-fill" 
                                                     style="width: @(((double)(currentChallengeIndex + 1) / challenges.Count) * 100)%"></div>
                                            </div>
                                            <p class="text-center text-sm text-gray-600 mt-2">
                                                Progress: @(currentChallengeIndex + 1) of @challenges.Count challenges
                                            </p>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        }
                        else
                        {
                            <!-- Challenge Complete -->
                            <div class="challenge-complete">
                                <div class="completion-card">
                                    <div class="completion-icon">🏆</div>
                                    <h3 class="text-2xl font-bold text-yellow-800 mb-2">
                                        Amazing Work, Language Explorer!
                                    </h3>
                                    <p class="text-yellow-700 mb-4">
                                        You completed @challenges.Count language challenges and got @correctAnswers correct!
                                    </p>
                                    
                                    <div class="completion-stats">
                                        <div class="stat-item">
                                            <span class="stat-number">@correctAnswers</span>
                                            <span class="stat-label">Correct</span>
                                        </div>
                                        <div class="stat-item">
                                            <span class="stat-number">@((int)((double)correctAnswers / challenges.Count * 100))%</span>
                                            <span class="stat-label">Accuracy</span>
                                        </div>
                                        <div class="stat-item">
                                            <span class="stat-number">@languagesLearned.Count</span>
                                            <span class="stat-label">Languages</span>
                                        </div>
                                    </div>

                                    <div class="completion-rewards">
                                        <h4 class="font-bold text-green-800 mb-2">🎁 Your Rewards:</h4>
                                        <ul class="rewards-list">
                                            <li>💰 +@bonusIncome bonus income!</li>
                                            <li>⭐ +@bonusReputation reputation points!</li>
                                            <li>🧠 Language learning achievement!</li>
                                        </ul>
                                    </div>

                                    <div class="completion-actions mt-6">
                                        <button @onclick="TryAgain" class="btn-child-friendly bg-purple-500 mr-2">
                                            🔄 Practice Again
                                        </button>
                                        <button @onclick="CloseChallenge" class="btn-child-friendly bg-blue-500">
                                            ✨ Continue Playing
                                        </button>
                                    </div>
                                </div>
                            </div>
                        }
                    }
                    else
                    {
                        <!-- No Challenges Available -->
                        <div class="no-challenges">
                            <div class="no-challenges-card">
                                <div class="text-6xl mb-4">🗺️</div>
                                <h3 class="text-xl font-bold text-gray-800 mb-2">No Language Challenges Available</h3>
                                <p class="text-gray-600 mb-4">
                                    You need to own territories to practice their languages!
                                </p>
                                <p class="text-blue-600 text-sm">
                                    💡 Acquire some territories first, then come back to learn their languages.
                                </p>
                                <button @onclick="CloseChallenge" class="btn-child-friendly bg-blue-500 mt-4">
                                    🏴 Go Get Some Territories!
                                </button>
                            </div>
                        </div>
                    }
                </div>
            </div>
        </div>
    }
</div>

@code {
    // Educational constants for child-friendly learning
    private const double PRONUNCIATION_ACCURACY_THRESHOLD = 0.7; // 70% similarity threshold for children
    private const int FEEDBACK_DISPLAY_DURATION_MS = 3000; // 3 seconds to show feedback before auto-advancing
    
    [Parameter] public bool IsVisible { get; set; }
    [Parameter] public Guid PlayerId { get; set; }
    [Parameter] public EventCallback OnClose { get; set; }
    [Parameter] public EventCallback<(int bonusIncome, int bonusReputation)> OnRewardsEarned { get; set; }

    // Challenge state
    private List<LanguageChallengeDto> challenges = new();
    private LanguageChallengeDto? currentChallenge;
    private int currentChallengeIndex = 0;
    private int correctAnswers = 0;
    private HashSet<string> languagesLearned = new();
    private bool isLoading = false;
    private bool isSubmitting = false;
    private bool isCorrect = false;
    private string userInput = "";
    private string feedbackMessage = "";
    private int bonusIncome = 0;
    private int bonusReputation = 0;

    // Speech recognition state
    private bool speechRecognitionSupported = false;
    private bool useSpeechInput = false;
    private bool isRecording = false;
    private bool hasRecordedAudio = false;
    private byte[]? recordedAudioData = null;
    private double recordedAudioDuration = 0.0;
    private IJSObjectReference? jsModule;
    private DotNetObjectReference<LanguageLearningChallenge>? dotNetRef;
    private bool _disposed = false;
    private CancellationTokenSource _cancellationTokenSource = new();

    private bool isVisible => IsVisible;

    protected override async Task OnInitializedAsync()
    {
        // Create .NET object reference for JavaScript callbacks
        dotNetRef = DotNetObjectReference.Create(this);

        // Load JavaScript module for speech recording
        try
        {
            jsModule = await JSRuntime.InvokeAsync<IJSObjectReference>("import", "./js/speech-recording.js");
            await jsModule.InvokeVoidAsync("speechRecorderInterop.init", dotNetRef);
            
            // Check if browser supports MediaRecorder API
            var browserSupported = await jsModule.InvokeAsync<bool>("speechRecorderInterop.isSupported");
            
            Logger.LogInformation("Browser MediaRecorder support: {Supported}", browserSupported);
        }
        catch (Exception ex)
        {
            Logger.LogError(ex, "Error loading speech recording JavaScript module");
        }

        // Check if speech recognition service is supported
        try
        {
            var healthStatus = await SpeechRecognitionService.CheckServiceHealthAsync();
            var serviceSupported = healthStatus.IsAvailable;
            
            // Both browser and service must support speech recognition
            speechRecognitionSupported = (jsModule != null) && serviceSupported;
            
            if (speechRecognitionSupported)
            {
                Logger.LogInformation("Speech recognition is fully available for language learning (browser + service)");
            }
            else
            {
                Logger.LogWarning("Speech recognition not fully available - Browser: {Browser}, Service: {Service}", 
                    jsModule != null, serviceSupported);
            }
        }
        catch (Exception ex)
        {
            Logger.LogError(ex, "Error checking speech recognition availability");
            speechRecognitionSupported = false;
        }
    }

    protected override async Task OnParametersSetAsync()
    {
        if (isVisible && PlayerId != Guid.Empty && !challenges.Any())
        {
            await LoadLanguageChallenges();
        }
    }

    private async Task LoadLanguageChallenges()
    {
        try
        {
            isLoading = true;
            StateHasChanged();

            challenges = await TerritoryService.GetTerritoryLanguageChallengesAsync(PlayerId);
            
            if (challenges.Any())
            {
                currentChallengeIndex = 0;
                currentChallenge = challenges[0];
                correctAnswers = 0;
                languagesLearned.Clear();
            }

            Logger.LogInformation("Loaded {Count} language challenges for player {PlayerId}", 
                challenges.Count, PlayerId);
        }
        catch (Exception ex)
        {
            Logger.LogError(ex, "Error loading language challenges for player {PlayerId}", PlayerId);
        }
        finally
        {
            isLoading = false;
            if (!_disposed)
            {
                StateHasChanged();
            }
        }
    }

    private async Task CheckAnswer()
    {
        if (currentChallenge == null || _disposed) return;

        try
        {
            isSubmitting = true;
            if (!_disposed)
            {
                StateHasChanged();
            }

            if (useSpeechInput && hasRecordedAudio && recordedAudioData != null)
            {
                // Use speech recognition for pronunciation assessment
                var pronunciationResult = await SpeechRecognitionService.AssessPronunciationAsync(
                    recordedAudioData, currentChallenge.Word, currentChallenge.LanguageCode);

                isCorrect = pronunciationResult.Passed;
                feedbackMessage = pronunciationResult.ChildFriendlyFeedback;

                Logger.LogInformation("Speech pronunciation assessment: Score={Score}, Passed={Passed}", 
                    pronunciationResult.OverallScore, pronunciationResult.Passed);
            }
            else if (!useSpeechInput && !string.IsNullOrWhiteSpace(userInput))
            {
                // Use text similarity checking for typed input
                isCorrect = CheckPronunciation(userInput.Trim(), currentChallenge.Word, currentChallenge.Pronunciation);
                
                if (isCorrect)
                {
                    feedbackMessage = GetPositiveFeedback();
                }
                else
                {
                    feedbackMessage = GetEncouragingFeedback();
                }
            }
            else
            {
                feedbackMessage = "Please provide input (speech or text) to check your pronunciation!";
                isCorrect = false;
            }
            
            if (isCorrect)
            {
                correctAnswers++;
                languagesLearned.Add(currentChallenge.LanguageCode);
            }

            if (!_disposed)
            {
                StateHasChanged();
            }
            
            // Auto-advance after showing feedback
            try
            {
                await Task.Delay(FEEDBACK_DISPLAY_DURATION_MS, _cancellationTokenSource.Token);
                if (!_disposed && !_cancellationTokenSource.Token.IsCancellationRequested)
                {
                    await NextChallenge();
                }
            }
            catch (OperationCanceledException)
            {
                // Component was disposed during delay - this is expected
                return;
            }
        }
        catch (Exception ex)
        {
            Logger.LogError(ex, "Error checking language answer for challenge {LanguageCode}", 
                currentChallenge?.LanguageCode);
            feedbackMessage = "Sorry, we had trouble checking your answer. Please try again!";
        }
        finally
        {
            isSubmitting = false;
            if (!_disposed)
            {
                StateHasChanged();
            }
        }
    }

    private void SetInputMode(bool useSpeech)
    {
        useSpeechInput = useSpeech && speechRecognitionSupported;
        
        // Clear previous input when switching modes
        userInput = "";
        hasRecordedAudio = false;
        recordedAudioData = null;
        feedbackMessage = "";
        
        StateHasChanged();
    }

    private async Task StartRecording()
    {
        if (jsModule == null)
        {
            Logger.LogError("JavaScript module not loaded for speech recording");
            feedbackMessage = "Speech recording not available. Please try typing your answer instead!";
            StateHasChanged();
            return;
        }

        try
        {
            isRecording = true;
            hasRecordedAudio = false;
            feedbackMessage = "";
            StateHasChanged();

            Logger.LogInformation("Starting real microphone recording for language challenge");
            
            // Start real microphone recording via JavaScript interop
            var success = await jsModule.InvokeAsync<bool>("speechRecorderInterop.startRecording");
            
            if (!success)
            {
                isRecording = false;
                feedbackMessage = "Could not start recording. Please check microphone permissions and try again!";
                StateHasChanged();
            }
        }
        catch (Exception ex)
        {
            Logger.LogError(ex, "Error starting real microphone recording");
            isRecording = false;
            feedbackMessage = "Recording failed to start. Please try typing your answer instead!";
            StateHasChanged();
        }
    }

    private async Task StopRecording()
    {
        if (jsModule == null || !isRecording)
        {
            return;
        }

        try
        {
            Logger.LogInformation("Stopping real microphone recording for language challenge");
            
            // Stop real microphone recording via JavaScript interop
            var success = await jsModule.InvokeAsync<bool>("speechRecorderInterop.stopRecording");
            
            if (!success)
            {
                feedbackMessage = "Recording stopped with an error. Please try again!";
                StateHasChanged();
            }
            
            // Note: isRecording will be set to false in OnAudioRecorded callback
        }
        catch (Exception ex)
        {
            Logger.LogError(ex, "Error stopping real microphone recording");
            isRecording = false;
            feedbackMessage = "Recording stopped with an error. Please try again!";
            StateHasChanged();
        }
    }

    private async Task SkipChallenge()
    {
        feedbackMessage = "That's okay! Every language takes practice. Let's try another one! 💪";
        StateHasChanged();
        
        await Task.Delay(2000);
        await NextChallenge();
    }

    private async Task NextChallenge()
    {
        userInput = "";
        feedbackMessage = "";
        isCorrect = false;
        hasRecordedAudio = false;
        recordedAudioData = null;

        if (currentChallengeIndex < challenges.Count - 1)
        {
            currentChallengeIndex++;
            currentChallenge = challenges[currentChallengeIndex];
        }
        else
        {
            // Challenge complete - calculate rewards
            await CompleteChallenge();
        }

        StateHasChanged();
    }

    private async Task CompleteChallenge()
    {
        try
        {
            // Calculate rewards based on performance
            var accuracyPercentage = (double)correctAnswers / challenges.Count;
            bonusIncome = (int)(correctAnswers * 50 * (1 + accuracyPercentage)); // Base 50 per correct, bonus for accuracy
            bonusReputation = Math.Min(10, correctAnswers * 2); // Max 10 reputation points

            currentChallenge = null; // This will show the completion screen

            // Notify parent component about rewards
            await OnRewardsEarned.InvokeAsync((bonusIncome, bonusReputation));

            Logger.LogInformation("Player {PlayerId} completed language challenge with {Correct}/{Total} correct answers", 
                PlayerId, correctAnswers, challenges.Count);
        }
        catch (Exception ex)
        {
            Logger.LogError(ex, "Error completing language challenge for player {PlayerId}", PlayerId);
        }
    }

    private void TryAgain()
    {
        currentChallengeIndex = 0;
        currentChallenge = challenges.FirstOrDefault();
        correctAnswers = 0;
        languagesLearned.Clear();
        userInput = "";
        feedbackMessage = "";
        isCorrect = false;
        bonusIncome = 0;
        bonusReputation = 0;
        hasRecordedAudio = false;
        recordedAudioData = null;
        StateHasChanged();
    }

    private async Task CloseChallenge()
    {
        await OnClose.InvokeAsync();
    }

    private async Task PlayAudio()
    {
        try
        {
            if (currentChallenge?.Word == null)
            {
                Logger.LogWarning("No current challenge word to play audio for");
                return;
            }

            // Use browser's Speech Synthesis API for educational pronunciation
            var utteranceOptions = new
            {
                text = currentChallenge.Word,
                lang = currentChallenge.LanguageCode ?? "en-US",
                rate = 0.8, // Slightly slower for learning
                pitch = 1.0,
                volume = 1.0
            };

            // Call JavaScript to use Speech Synthesis API
            await JSRuntime.InvokeVoidAsync("speechSynthesis.speak", 
                await JSRuntime.InvokeAsync<object>("createSpeechUtterance", utteranceOptions));

            Logger.LogInformation("Playing audio pronunciation for word: {Word} in language: {Language}", 
                currentChallenge.Word, currentChallenge.LanguageCode);

            // Provide visual feedback that audio is playing
            feedbackMessage = $"🔊 Listen carefully to the pronunciation of '{currentChallenge.Word}'!";
            StateHasChanged();
            
            // Clear feedback after a few seconds
            await Task.Delay(FEEDBACK_DISPLAY_DURATION_MS);
            if (feedbackMessage.StartsWith("🔊"))
            {
                feedbackMessage = "";
                StateHasChanged();
            }
        }
        catch (Exception ex)
        {
            Logger.LogError(ex, "Error playing audio for language challenge");
            feedbackMessage = "Sorry, audio playback is not available on this device. Try the pronunciation challenge instead!";
            StateHasChanged();
        }
    }

    private async Task HandleKeyPress(KeyboardEventArgs e)
    {
        if (e.Key == "Enter" && !string.IsNullOrWhiteSpace(userInput) && !isSubmitting)
        {
            await CheckAnswer();
        }
    }

    private bool CheckPronunciation(string userInput, string targetWord, string pronunciation)
    {
        // Simplified pronunciation checking for educational purposes
        // In a real implementation, this would use advanced speech recognition
        
        var cleanUserInput = userInput.ToLowerInvariant().Replace(" ", "");
        var cleanTargetWord = targetWord.ToLowerInvariant().Replace(" ", "");
        var cleanPronunciation = pronunciation.ToLowerInvariant().Replace("-", "").Replace(" ", "");

        // Check for exact match, close phonetic match, or reasonable attempt
        return cleanUserInput == cleanTargetWord || 
               cleanUserInput == cleanPronunciation ||
               IsCloseMatch(cleanUserInput, cleanTargetWord) ||
               IsCloseMatch(cleanUserInput, cleanPronunciation);
    }

    private bool IsCloseMatch(string input, string target)
    {
        if (input.Length == 0 || target.Length == 0) return false;
        
        // Simple Levenshtein distance check for close matches
        var distance = ComputeLevenshteinDistance(input, target);
        var maxLength = Math.Max(input.Length, target.Length);
        var similarity = 1.0 - (double)distance / maxLength;
        
        return similarity >= PRONUNCIATION_ACCURACY_THRESHOLD; // 70% similarity threshold for children
    }

    private int ComputeLevenshteinDistance(string s, string t)
    {
        var d = new int[s.Length + 1, t.Length + 1];

        for (int i = 0; i <= s.Length; i++)
            d[i, 0] = i;
        for (int j = 0; j <= t.Length; j++)
            d[0, j] = j;

        for (int i = 1; i <= s.Length; i++)
        {
            for (int j = 1; j <= t.Length; j++)
            {
                int cost = (t[j - 1] == s[i - 1]) ? 0 : 1;
                d[i, j] = Math.Min(Math.Min(d[i - 1, j] + 1, d[i, j - 1] + 1), d[i - 1, j - 1] + cost);
            }
        }

        return d[s.Length, t.Length];
    }

    private string GetPositiveFeedback()
    {
        var messages = new[]
        {
            "🌟 Excellent pronunciation! You're a natural language learner!",
            "🎉 Perfect! You nailed that pronunciation!",
            "✨ Amazing work! You're mastering languages like a true world leader!",
            "🚀 Fantastic! Your pronunciation skills are getting better!",
            "🏆 Outstanding! You're becoming a language expert!",
            "💫 Wonderful! Keep up the great language learning!"
        };
        return messages[Random.Shared.Next(messages.Length)];
    }

    private string GetEncouragingFeedback()
    {
        var messages = new[]
        {
            "💪 Good try! Language learning takes practice - you're doing great!",
            "🌱 Nice effort! Every attempt makes you better at languages!",
            "🎯 Keep going! You're learning something new with each try!",
            "✨ Great attempt! Languages are tricky but you're getting there!",
            "🌟 Don't worry! Even expert language learners needed practice!",
            "🚀 Good job trying! Each practice makes you stronger!"
        };
        return messages[Random.Shared.Next(messages.Length)];
    }

    // JavaScript Interop Callback Methods
    
    /// <summary>
    /// Called by JavaScript when audio recording is completed and processed
    /// Educational context: Real microphone audio converted to PCM format for Azure Speech Services
    /// </summary>
    /// <param name="base64AudioData">Base64 encoded PCM audio data</param>
    /// <param name="durationSeconds">Duration of the recording in seconds</param>
    [JSInvokable]
    public void OnAudioRecorded(string base64AudioData, double durationSeconds)
    {
        try
        {
            if (string.IsNullOrEmpty(base64AudioData))
            {
                Logger.LogWarning("Received empty audio data from JavaScript");
                isRecording = false;
                hasRecordedAudio = false;
                feedbackMessage = "No audio was captured. Please try recording again!";
                StateHasChanged();
                return;
            }

            // Convert base64 back to byte array
            recordedAudioData = Convert.FromBase64String(base64AudioData);
            recordedAudioDuration = durationSeconds;
            
            isRecording = false;
            hasRecordedAudio = true;
            
            Logger.LogInformation("Real microphone audio received: {Size} bytes, {Duration:F2} seconds", 
                recordedAudioData.Length, durationSeconds);
            
            // Provide encouraging feedback about successful recording
            if (durationSeconds < 0.5)
            {
                feedbackMessage = "Recording was very short. Try speaking a bit longer and click 'Check My Pronunciation'!";
            }
            else if (durationSeconds > 4.0)
            {
                feedbackMessage = "Great recording! That was nice and clear. Click 'Check My Pronunciation'!";
            }
            else
            {
                feedbackMessage = "Perfect! Audio recorded successfully. Click 'Check My Pronunciation'!";
            }

            StateHasChanged();
        }
        catch (Exception ex)
        {
            Logger.LogError(ex, "Error processing recorded audio data from JavaScript");
            isRecording = false;
            hasRecordedAudio = false;
            feedbackMessage = "Error processing your recording. Please try again!";
            StateHasChanged();
        }
    }

    /// <summary>
    /// Called by JavaScript when recording encounters an error
    /// Child-friendly error handling with helpful suggestions
    /// </summary>
    /// <param name="errorMessage">Error message from JavaScript</param>
    [JSInvokable]
    public void OnRecordingError(string errorMessage)
    {
        try
        {
            Logger.LogWarning("Recording error from JavaScript: {Error}", errorMessage);
            
            isRecording = false;
            hasRecordedAudio = false;
            feedbackMessage = errorMessage; // Already child-friendly from JavaScript
            
            StateHasChanged();
        }
        catch (Exception ex)
        {
            Logger.LogError(ex, "Error handling recording error callback");
        }
    }

    // IAsyncDisposable Implementation
    
    public async ValueTask DisposeAsync()
    {
        _disposed = true;
        
        try
        {
            // Cancel any ongoing delays or async operations
            _cancellationTokenSource?.Cancel();
            
            // Clean up JavaScript module and stop any ongoing recording
            if (jsModule != null)
            {
                await jsModule.InvokeVoidAsync("speechRecorderInterop.dispose");
                await jsModule.DisposeAsync();
            }

            // Dispose .NET object reference
            dotNetRef?.Dispose();
            
            // Dispose cancellation token source
            _cancellationTokenSource?.Dispose();
            
            Logger.LogInformation("Language learning challenge component disposed successfully");
        }
        catch (Exception ex)
        {
            Logger.LogError(ex, "Error disposing language learning challenge component");
        }
    }
}

<style>
.language-challenge-container {
    font-family: 'Comic Sans MS', cursive, sans-serif;
}

.modal-overlay {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: rgba(0, 0, 0, 0.5);
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 1rem;
    z-index: 50;
}

.modal-content {
    background: white;
    border-radius: 0.75rem;
    box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.25);
    max-width: 48rem;
    width: 100%;
    max-height: 100vh;
    overflow-y: auto;
}

.modal-header {
    background: #f3e8ff;
    padding: 1rem;
    border-bottom: 1px solid #c4b5fd;
    display: flex;
    justify-content: space-between;
    align-items: center;
}

.modal-body {
    padding: 1.5rem;
}

.close-button {
    color: #ef4444;
    font-size: 1.25rem;
    font-weight: 700;
    transition: color 0.2s;
    background: none;
    border: none;
    cursor: pointer;
}

.close-button:hover {
    color: #dc2626;
}

.challenge-header {
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
    margin-bottom: 1.5rem;
}

.score-circle {
    background: #dcfce7;
    border-radius: 50%;
    padding: 1rem;
    text-align: center;
    min-width: 4rem;
}

.word-card {
    background: linear-gradient(to bottom right, #eff6ff, #f3e8ff);
    border-radius: 0.75rem;
    padding: 1.5rem;
    text-align: center;
}

.practice-input {
    width: 100%;
    padding: 0.75rem 1rem;
    border: 2px solid #c4b5fd;
    border-radius: 0.5rem;
    font-size: 1.125rem;
}

.practice-input:focus {
    border-color: #8b5cf6;
    outline: none;
    box-shadow: 0 0 0 3px rgba(139, 92, 246, 0.1);
}

.btn-child-friendly {
    padding: 0.5rem 1rem;
    border-radius: 0.5rem;
    font-weight: 700;
    color: white;
    transition: all 0.2s;
    border: none;
    cursor: pointer;
}

.btn-child-friendly:hover {
    transform: scale(1.05);
}

.btn-child-friendly:active {
    transform: scale(0.95);
}

.btn-child-friendly:disabled {
    opacity: 0.5;
    cursor: not-allowed;
    transform: none;
}

.feedback-card {
    border-radius: 0.5rem;
    padding: 1rem;
    display: flex;
    align-items: flex-start;
    gap: 0.75rem;
}

.feedback-card.correct {
    background: #f0fdf4;
    border: 2px solid #86efac;
}

.feedback-card.incorrect {
    background: #fffbeb;
    border: 2px solid #fcd34d;
}

.feedback-icon {
    font-size: 1.5rem;
}

.feedback-message {
    font-weight: 600;
}

.feedback-card.correct .feedback-message {
    color: #166534;
}

.feedback-card.incorrect .feedback-message {
    color: #92400e;
}

.feedback-hint {
    font-size: 0.875rem;
    margin-top: 0.25rem;
}

.feedback-card.incorrect .feedback-hint {
    color: #d97706;
}

.progress-bar {
    width: 100%;
    background: #e5e7eb;
    border-radius: 9999px;
    height: 0.75rem;
}

.progress-fill {
    background: #8b5cf6;
    height: 0.75rem;
    border-radius: 9999px;
    transition: width 0.5s;
}

.completion-card, .no-challenges-card {
    background: linear-gradient(to bottom right, #fffbeb, #fed7aa);
    border-radius: 0.75rem;
    padding: 2rem;
    text-align: center;
}

.completion-icon {
    font-size: 3.75rem;
    margin-bottom: 1rem;
}

.completion-stats {
    display: flex;
    justify-content: center;
    gap: 2rem;
    margin: 1.5rem 0;
}

.stat-item {
    text-align: center;
}

.stat-number {
    display: block;
    font-size: 1.5rem;
    font-weight: 700;
    color: #1e40af;
}

.stat-label {
    font-size: 0.875rem;
    color: #2563eb;
}

.rewards-list {
    text-align: left;
    max-width: 20rem;
    margin: 0 auto;
    list-style: none;
    padding: 0;
}

.rewards-list li {
    color: #166534;
    font-weight: 500;
    margin: 0.5rem 0;
}

/* Speech Recognition Styles */
.speech-options {
    background: #f8fafc;
    border-radius: 0.5rem;
    padding: 1rem;
    border: 2px solid #e2e8f0;
}

.speech-mode-selector {
    display: flex;
    gap: 0.5rem;
    margin-bottom: 0.5rem;
}

.btn-mode {
    padding: 0.5rem 1rem;
    border: 2px solid #cbd5e1;
    border-radius: 0.375rem;
    background: white;
    color: #64748b;
    font-weight: 600;
    cursor: pointer;
    transition: all 0.2s;
}

.btn-mode.active {
    background: #8b5cf6;
    color: white;
    border-color: #8b5cf6;
}

.btn-mode:hover:not(.active) {
    background: #f1f5f9;
    border-color: #94a3b8;
}

.speech-unavailable {
    padding: 0.5rem;
    background: #fed7aa;
    border-radius: 0.375rem;
    text-align: center;
}

.speech-recording {
    text-align: center;
    padding: 1rem;
}

.recording-controls {
    margin-bottom: 1rem;
}

.record-button {
    font-size: 1.125rem;
    padding: 1rem 2rem;
    min-width: 12rem;
}

.record-button.recording {
    animation: pulse 1.5s infinite;
}

.recording-status {
    margin: 0.5rem 0;
    padding: 0.5rem;
    background: #dcfce7;
    border-radius: 0.375rem;
}

.recording-indicator {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.5rem;
    margin-top: 1rem;
}

.pulse-animation {
    width: 1rem;
    height: 1rem;
    background: #ef4444;
    border-radius: 50%;
    animation: pulse 1s infinite;
}

@@keyframes pulse {
    0% {
        transform: scale(1);
        opacity: 1;
    }
    50% {
        transform: scale(1.1);
        opacity: 0.7;
    }
    100% {
        transform: scale(1);
        opacity: 1;
    }
}
</style>