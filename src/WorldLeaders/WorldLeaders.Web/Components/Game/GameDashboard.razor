@page "/game"
@rendermode InteractiveServer
@using WorldLeaders.Shared.DTOs
@using WorldLeaders.Shared.Enums
@using WorldLeaders.Shared.Services
@using WorldLeaders.Shared.Models
@using WorldLeaders.Web.Services
@using Microsoft.AspNetCore.SignalR.Client
@using Microsoft.AspNetCore.Components
@using System.Web
@inject IHttpClientFactory HttpClientFactory
@inject ILogger<GameDashboard> Logger
@inject IHubConnectionFactory HubConnectionFactory
@inject IAuthenticationClientService AuthService
@inject ILocalStorageService LocalStorage
@inject NavigationManager Navigation
@inject ICharacterPersonaService PersonaService
@implements IAsyncDisposable

<PageTitle>World Leaders Game - Dashboard</PageTitle>

<div class="retro-game-container">
    <div class="max-w-6xl mx-auto p-4">
        <!-- Header Section - Improved Layout with Better Spacing -->
        <div class="pixel-art-card green-theme mb-6">
            <div class="flex flex-col md:flex-row md:items-start md:justify-between gap-6">
                <div class="flex-1">
                    <h1 class="retro-title-compact mb-3">🌍 WORLD LEADERS</h1>
                    <p class="retro-body text-retro-sm">Educational game for young leaders (Age 12+)</p>
                </div>
                <div class="text-left md:text-right md:min-w-0 md:flex-shrink-0">
                    @if (currentUser != null)
                    {
                        <h2 class="retro-subtitle-compact mb-2">Welcome, @currentUser.Username!</h2>
                        <p class="retro-body text-retro-xs opacity-75 mb-1">@currentUser.Email</p>
                        <p class="retro-body text-retro-xs opacity-75 mb-3">Last login: @currentUser.LastLoginAt.ToString("MMM dd, yyyy")</p>
                        <button @onclick="Logout" class="pixel-art-button-small red px-3 py-2">
                            LOGOUT
                        </button>
                    }
                    else if (playerDashboard != null)
                    {
                        <h2 class="retro-subtitle-compact mb-2">Welcome, @playerDashboard.Username!</h2>
                        <p class="retro-body text-retro-xs">@playerDashboard.CurrentGameState</p>
                    }
                    else if (!isLoading)
                    {
                        <button @onclick="GoToLogin" class="pixel-art-button px-4 py-2">
                            LOGIN TO PLAY
                        </button>
                    }
                </div>
            </div>
        </div>

        @if (playerDashboard != null)
        {
            <!-- Game Interface - Available for both authenticated and guest users -->
            @if (isAuthenticated && currentUser != null)
            {
                <!-- Resource Management Framework -->
                <ResourceManager Income="@playerDashboard.Income" 
                               Reputation="@playerDashboard.Reputation" 
                               Happiness="@playerDashboard.Happiness"
                               RecentChanges="@recentResourceChanges" />

                <!-- Interactive Dice Rolling System -->
                <InteractiveDiceRoller PlayerId="@playerDashboard.Id" 
                                     OnDiceRolled="@OnDiceRolled" />

                <!-- Territory Displayer Enhanced - World Geography Learning -->
                <TerritoryDisplayer PlayerId="@playerDashboard.Id" 
                                  PlayerReputation="@playerDashboard.Reputation"
                                  PlayerIncome="@playerDashboard.Income"
                                  OnTerritorySelected="@OnTerritoryPurchased" />

                <!-- Language Learning Challenge - Speech Recognition -->
                <div class="bg-white rounded-lg shadow-lg p-6 mb-6">
                    <div class="flex items-center justify-between mb-4">
                        <h3 class="text-xl font-bold text-purple-800">🗣️ Language Learning</h3>
                        <button @onclick="@(() => showLanguageChallenge = true)" 
                                class="bg-gradient-to-r from-purple-500 to-pink-500 text-white font-bold py-2 px-4 rounded-lg hover:from-purple-600 hover:to-pink-600 transition-all duration-300">
                            Practice Speaking
                        </button>
                    </div>
                    <p class="text-gray-600">Practice pronouncing words from your territories and learn about different cultures!</p>
                </div>

                <!-- Language Learning Challenge Modal -->
                <LanguageLearningChallenge IsVisible="@showLanguageChallenge" 
                                         PlayerId="@playerDashboard.Id"
                                         OnClose="@(() => showLanguageChallenge = false)" />

                <!-- AI Learning Assistants -->
                <div class="bg-white rounded-lg shadow-lg p-6 mb-6">
                    <div class="flex items-center justify-between mb-4">
                        <h3 class="text-xl font-bold text-indigo-800">🤖 AI Learning Assistants</h3>
                        <button @onclick="@(() => showAIChat = true)" 
                                class="bg-gradient-to-r from-indigo-500 to-purple-500 text-white font-bold py-2 px-4 rounded-lg hover:from-indigo-600 hover:to-purple-600 transition-all duration-300">
                            Chat with AI Guides
                        </button>
                    </div>
                    <p class="text-gray-600">Get personalized advice from AI mentors specializing in careers, territories, languages, and leadership strategy!</p>
                </div>

                <!-- AI Agent Chat Modal -->
                <AIAgentChat IsVisible="@showAIChat" 
                            PlayerId="@playerDashboard.Id"
                            OnClose="@(() => showAIChat = false)" />
            }
            else
            {
                <!-- Guest User Game Interface - Simplified version for non-authenticated users -->
                <div class="bg-blue-50 rounded-lg p-4 mb-6 border border-blue-200">
                    <div class="flex items-center justify-between">
                        <div>
                            <h3 class="text-lg font-bold text-blue-800">🎮 Playing as Guest</h3>
                            <p class="text-sm text-blue-600">Your progress is saved temporarily. Create an account to save permanently!</p>
                        </div>
                        <button @onclick="GoToLogin" class="bg-blue-600 text-white font-bold py-2 px-4 rounded-lg hover:bg-blue-700 transition-colors">
                            Create Account
                        </button>
                    </div>
                </div>

                <!-- Simple Resource Display for Guest Users -->
                <div class="bg-white rounded-lg shadow-lg p-6 mb-6">
                    <h3 class="text-xl font-bold text-gray-800 mb-4">📊 Your Stats</h3>
                    <div class="grid grid-cols-1 md:grid-cols-3 gap-4">
                        <div class="text-center p-3 bg-green-50 rounded-lg">
                            <div class="text-2xl font-bold text-green-600">💰 $@playerDashboard.Income.ToString("N0")</div>
                            <div class="text-sm text-green-700">Monthly Income</div>
                        </div>
                        <div class="text-center p-3 bg-blue-50 rounded-lg">
                            <div class="text-2xl font-bold text-blue-600">⭐ @playerDashboard.Reputation%</div>
                            <div class="text-sm text-blue-700">Reputation</div>
                        </div>
                        <div class="text-center p-3 bg-purple-50 rounded-lg">
                            <div class="text-2xl font-bold text-purple-600">😊 @playerDashboard.Happiness%</div>
                            <div class="text-sm text-purple-700">Happiness</div>
                        </div>
                    </div>
                    <div class="mt-4 text-center p-3 bg-yellow-50 rounded-lg">
                        <div class="text-lg font-bold text-yellow-600">@playerDashboard.CurrentJob</div>
                        <div class="text-sm text-yellow-700">Current Job</div>
                    </div>
                </div>

                <!-- Simple Dice Rolling for Guest Users -->
                <div class="bg-white rounded-lg shadow-lg p-6 mb-6">
                    <h3 class="text-xl font-bold text-gray-800 mb-4">🎲 Career Advancement</h3>
                    <div class="text-center">
                        <button @onclick="RollDice" 
                                class="bg-gradient-to-r from-purple-500 to-blue-500 text-white font-bold py-3 px-6 rounded-lg shadow-lg hover:from-purple-600 hover:to-blue-600 transition-all duration-300 transform hover:scale-105">
                            <i class="fas fa-dice mr-2"></i>
                            Roll Career Dice
                        </button>
                        <p class="text-gray-600 mt-2">Roll the dice to advance your career and increase your income!</p>
                    </div>
                </div>

                <!-- Territory Displayer for Guest Users -->
                <TerritoryDisplayer PlayerId="@playerDashboard.Id" 
                                  PlayerReputation="@playerDashboard.Reputation"
                                  PlayerIncome="@playerDashboard.Income"
                                  OnTerritorySelected="@OnTerritoryPurchased" />
            }

            <!-- Game Events Section (shared between authenticated and guest users) -->
            @if (gameEvents.Any())
            {
                <div class="bg-white rounded-lg shadow-lg p-6 mb-6">
                    <h3 class="text-xl font-bold text-gray-800 mb-4">📰 Recent Events</h3>
                    <div class="space-y-4">
                        @foreach (var gameEvent in gameEvents.Take(3))
                        {
                            <div class="@GetEventCardClass(gameEvent.IsPositive) rounded-lg p-4">
                                <div class="flex items-start">
                                    <span class="text-2xl mr-3">@gameEvent.IconEmoji</span>
                                    <div>
                                        <h4 class="font-bold text-gray-800">@gameEvent.Title</h4>
                                        <p class="text-gray-600 text-sm mb-2">@gameEvent.Description</p>
                                        <div class="flex space-x-4 text-xs">
                                            @if (gameEvent.IncomeEffect != 0)
                                            {
                                                <span class="@GetEffectClass(gameEvent.IncomeEffect)">
                                                    💰 @FormatEffect(gameEvent.IncomeEffect)
                                                </span>
                                            }
                                            @if (gameEvent.ReputationEffect != 0)
                                            {
                                                <span class="@GetEffectClass(gameEvent.ReputationEffect)">
                                                    ⭐ @FormatEffect(gameEvent.ReputationEffect)
                                                </span>
                                            }
                                            @if (gameEvent.HappinessEffect != 0)
                                            {
                                                <span class="@GetEffectClass(gameEvent.HappinessEffect)">
                                                    😊 @FormatEffect(gameEvent.HappinessEffect)
                                                </span>
                                            }
                                        </div>
                                    </div>
                                </div>
                            </div>
                        }
                    </div>
                </div>
            }

            <!-- Message Display -->
            @if (!string.IsNullOrEmpty(gameMessage))
            {
                <div class="bg-blue-100 border-l-4 border-blue-500 p-4 mb-6 rounded-lg">
                    <p class="text-blue-800 font-medium">@gameMessage</p>
                </div>
            }
        }
        else if (isAuthenticated && currentUser != null)
        {
            <!-- Authenticated User without playerDashboard - Character Selection -->
            <div class="bg-white rounded-lg shadow-lg p-8 text-center">
                <h2 class="text-2xl font-bold text-gray-800 mb-4">🎮 Welcome to World Leaders Game!</h2>
                <p class="text-gray-600 mb-6">You're logged in as <strong>@currentUser.Username</strong>. Your educational leadership journey awaits!</p>
                
                <div class="bg-blue-50 rounded-lg p-6 mb-6">
                    <h3 class="text-lg font-semibold text-blue-800 mb-3">🎯 Your Learning Profile</h3>
                    <div class="grid grid-cols-1 md:grid-cols-2 gap-4 text-left">
                        <div>
                            <p><strong>Username:</strong> @currentUser.Username</p>
                            <p><strong>Email:</strong> @currentUser.Email</p>
                            <p><strong>Last Login:</strong> @currentUser.LastLoginAt.ToString("MMM dd, yyyy")</p>
                        </div>
                        <div>
                            <p><strong>Game Status:</strong> Ready to Play</p>
                            <p><strong>Learning Focus:</strong> Geography, Economics, Languages</p>
                            <p><strong>Age Group:</strong> 12+ Educational Content</p>
                        </div>
                    </div>
                </div>
                
                <div class="max-w-md mx-auto">
                    @if (selectedCharacterPersona != null)
                    {
                        <!-- Character Persona Display -->
                        <div class="pixel-art-card mb-6" style="border-color: @selectedCharacterPersona.PrimaryColor;">
                            <div class="text-center">
                                <h3 class="retro-subtitle mb-4">YOUR CHOSEN CHARACTER</h3>
                                <div class="flex items-center justify-center gap-4 mb-4">
                                    <img src="@selectedCharacterPersona.PixelArtSprite64" 
                                         alt="@selectedCharacterPersona.Name" 
                                         class="pixel-image w-20 h-20" />
                                    <div class="text-left">
                                        <h4 class="font-bold font-retro text-lg" style="color: @selectedCharacterPersona.PrimaryColor;">
                                            @selectedCharacterPersona.Name.ToUpper()
                                        </h4>
                                        <p class="retro-body text-sm mb-2">@selectedCharacterPersona.Description</p>
                                        <div class="trait-badge mb-2">@selectedCharacterPersona.PersonalityTrait</div>
                                        <p class="text-xs font-retro" style="color: @selectedCharacterPersona.PrimaryColor;">
                                            Special Ability: @selectedCharacterPersona.SpecialAbility
                                        </p>
                                    </div>
                                </div>
                            </div>
                        </div>
                    }
                    
                    @if (!string.IsNullOrEmpty(errorMessage))
                    {
                        <div class="mt-2 text-red-600 text-sm">@errorMessage</div>
                    }
                    
                    <button @onclick="CreateAuthenticatedPlayer" 
                            disabled="@(selectedCharacterPersona == null || isLoading)"
                            class="w-full mt-4 pixel-art-button text-lg px-8 py-4"
                            style="background-color: @(selectedCharacterPersona?.PrimaryColor ?? "#2ea44f");">
                        @if (isLoading)
                        {
                            <span class="flex items-center justify-center">
                                <span class="animate-spin mr-2">⭐</span>
                                Creating your character...
                            </span>
                        }
                        else
                        {
                            <span>🚀 START ADVENTURE AS @(selectedCharacterPersona?.Name.ToUpper() ?? "CHARACTER")</span>
                        }
                    </button>
                    
                    <div class="text-center mt-4">
                        <button @onclick="ChangeCharacter" 
                                class="pixel-art-button px-4 py-2 text-sm"
                                style="background-color: var(--pixel-gray);">
                            🎭 CHANGE CHARACTER
                        </button>
                    </div>
                    
                    <p class="mt-3 text-xs text-gray-500 text-center">Ready to begin learning about world leadership, geography, and languages with @(selectedCharacterPersona?.Name ?? "your character")!</p>
                </div>
            </div>
        }
        else if (isLoading)
        {
            <div class="flex justify-center items-center h-64">
                <div class="animate-spin rounded-full h-12 w-12 border-b-2 border-blue-500"></div>
                <span class="ml-4 text-xl text-gray-600">Loading...</span>
            </div>
        }
        else
        {
            <!-- Not Authenticated - Show Login Prompt -->
            <div class="bg-white rounded-lg shadow-lg p-8 text-center">
                <h2 class="text-2xl font-bold text-gray-800 mb-4">🔐 Authentication Required</h2>
                <p class="text-gray-600 mb-6">Please log in to access the World Leaders Game and start your educational journey!</p>
                
                <div class="bg-yellow-50 rounded-lg p-6 mb-6">
                    <h3 class="text-lg font-semibold text-yellow-800 mb-3">🎯 What You'll Learn</h3>
                    <div class="grid grid-cols-1 md:grid-cols-3 gap-4 text-left">
                        <div class="text-center">
                            <div class="text-2xl mb-2">🌍</div>
                            <strong>Geography</strong>
                            <p class="text-sm text-gray-600">Learn about countries, capitals, and cultures</p>
                        </div>
                        <div class="text-center">
                            <div class="text-2xl mb-2">💰</div>
                            <strong>Economics</strong>
                            <p class="text-sm text-gray-600">Understand GDP, resources, and strategy</p>
                        </div>
                        <div class="text-center">
                            <div class="text-2xl mb-2">🗣️</div>
                            <strong>Languages</strong>
                            <p class="text-sm text-gray-600">Practice pronunciation and cultural awareness</p>
                        </div>
                    </div>
                </div>
                
                <div class="space-y-4">
                    <button @onclick="GoToLogin" class="w-full bg-gradient-to-r from-blue-500 to-purple-500 text-white font-bold py-3 px-6 rounded-lg shadow-lg hover:from-blue-600 hover:to-purple-600 transition-all duration-300 transform hover:scale-105">
                        🚀 Login to Start Playing
                    </button>
                    
                    <p class="text-sm text-gray-500">
                        New to the game? <button @onclick="GoToLogin" class="text-blue-600 hover:text-blue-800 font-medium">Create an account</button> to begin your educational adventure!
                    </p>
                </div>
            </div>
        }
    </div>
</div>

@code {
    private PlayerDashboardDto? playerDashboard;
    private UserInfoDto? currentUser;
    private bool isAuthenticated = false;
    private List<GameEventDto> gameEvents = new();
    private List<ResourceChangeRecord> recentResourceChanges = new();
    private CharacterPersona? selectedCharacterPersona;
    private string newPlayerName = "";
    private string gameMessage = "";
    private string errorMessage = "";
    private bool isLoading = false;
    private bool showLanguageChallenge = false;
    private bool showAIChat = false;
    private HubConnection? hubConnection;

    protected override async Task OnInitializedAsync()
    {
        // Check authentication first
        isAuthenticated = await AuthService.IsAuthenticatedAsync();
        
        // Try to load selected character persona from multiple sources
        await LoadSelectedCharacterAsync();
        
        // If no character selected, redirect to character selection (prevent infinite loops)
        if (selectedCharacterPersona == null)
        {
            Logger.LogInformation("No character selected, redirecting to character selection");
            var currentPath = Navigation.ToAbsoluteUri(Navigation.Uri).AbsolutePath;
            if (currentPath != "/character-select")
            {
                Navigation.NavigateTo("/character-select");
            }
            else
            {
                Logger.LogWarning("Already on character selection page but no character loaded - preventing redirect loop");
                errorMessage = "Please select a character to continue.";
            }
            return;
        }
        
        Logger.LogInformation("Character loaded: {CharacterName} ({CharacterId})", selectedCharacterPersona.Name, selectedCharacterPersona.Id);
        
        // Check if player already exists for this character
        await LoadExistingPlayerAsync();
        
        if (playerDashboard == null)
        {
            // No existing player, create one automatically
            if (isAuthenticated)
            {
                // Load current user information
                currentUser = await AuthService.GetCurrentUserAsync();
                
                if (currentUser != null)
                {
                    gameMessage = $"Welcome back, {currentUser.Username}! Ready to continue your leadership journey with {selectedCharacterPersona.Name}? 🎉";
                    
                    // Auto-create player for authenticated users
                    await CreateAuthenticatedPlayer();
                }
            }
            else
            {
                // Auto-create player for non-authenticated users
                await CreatePlayerWithCharacter();
            }
        }
        else
        {
            Logger.LogInformation("Existing player found: {PlayerId} for character {CharacterName}", playerDashboard.Id, selectedCharacterPersona.Name);
            gameMessage = $"Welcome back, {selectedCharacterPersona.Name}! Your leadership journey continues! 🎉";
        }
        
        await LoadGameEvents();
        
        // Set up SignalR connection
        try
        {
            hubConnection = HubConnectionFactory.CreateConnection();
            
            hubConnection.On<GameStateUpdate>("GameStateUpdated", (update) =>
            {
                InvokeAsync(() =>
                {
                    gameMessage = update.Message;
                    StateHasChanged();
                });
            });

            await hubConnection.StartAsync();
        }
        catch (Exception ex)
        {
            // SignalR connection failed - continue without real-time updates
            Logger.LogWarning(ex, "SignalR connection failed, continuing without real-time updates");
        }
    }

    private async Task CreateAuthenticatedPlayer()
    {
        // Clear previous errors
        errorMessage = "";
        
        // Validate character persona is selected
        if (selectedCharacterPersona == null)
        {
            errorMessage = "Please select a character to start the game.";
            Navigation.NavigateTo("/character-select");
            return;
        }

        isLoading = true;
        try
        {
            // Use authenticated HTTP client to create player linked to user account
            var httpClient = await AuthService.GetAuthenticatedHttpClientAsync();
            
            // Generate unique username to avoid conflicts when using same character multiple times
            var uniqueUsername = $"{currentUser?.Username ?? selectedCharacterPersona.Name}_{DateTime.UtcNow.Ticks % 10000}";
            var request = new CreatePlayerRequest(uniqueUsername, selectedCharacterPersona.Id);
            
            var response = await httpClient.PostAsJsonAsync("api/Game/players", request);
            if (response.IsSuccessStatusCode)
            {
                playerDashboard = await response.Content.ReadFromJsonAsync<PlayerDashboardDto>();
                if (playerDashboard != null)
                {
                    gameMessage = $"Welcome, {selectedCharacterPersona.Name}! Your authenticated leadership journey begins now! 🎉";
                    try
                    {
                        if (hubConnection != null)
                        {
                            await hubConnection.SendAsync("JoinGameSession", playerDashboard.Id.ToString());
                        }
                    }
                    catch
                    {
                        // SignalR connection failed, but player creation succeeded
                        // Continue without real-time updates
                    }
                }
                else
                {
                    errorMessage = "Failed to create your character. Please try again.";
                }
            }
            else
            {
                var errorContent = await response.Content.ReadAsStringAsync();
                errorMessage = response.StatusCode == System.Net.HttpStatusCode.Unauthorized 
                    ? "Your login session has expired. Please login again."
                    : $"Failed to create character: {response.StatusCode}";
            }
        }
        catch (HttpRequestException ex)
        {
            errorMessage = "Cannot connect to the game server. Please make sure the game is running.";
            Logger.LogWarning(ex, "Failed to connect to game server while creating authenticated player");
        }
        catch (Exception ex)
        {
            errorMessage = "An unexpected error occurred. Please try again.";
            Logger.LogError(ex, "Unexpected error while creating authenticated player");
        }
        finally
        {
            isLoading = false;
            StateHasChanged();
        }
    }

    private async Task CreatePlayer()
    {
        // Clear previous errors
        errorMessage = "";
        
        // Validate input
        if (string.IsNullOrWhiteSpace(newPlayerName))
        {
            errorMessage = "Please enter your name to start the game.";
            return;
        }

        var trimmedName = newPlayerName.Trim();
        if (trimmedName.Length < 2)
        {
            errorMessage = "Name must be at least 2 characters long.";
            return;
        }

        if (trimmedName.Length > 20)
        {
            errorMessage = "Name must be 20 characters or less.";
            return;
        }

        isLoading = true;
        try
        {
            var httpClient = HttpClientFactory.CreateClient("GameAPI");
            var request = new CreatePlayerRequest(trimmedName);
            
            var response = await httpClient.PostAsJsonAsync("api/Game/players", request);
            if (response.IsSuccessStatusCode)
            {
                playerDashboard = await response.Content.ReadFromJsonAsync<PlayerDashboardDto>();
                if (playerDashboard != null)
                {
                    gameMessage = $"Welcome, {playerDashboard.Username}! Your leadership journey begins now! 🎉";
                    try
                    {
                        if (hubConnection != null)
                        {
                            await hubConnection.SendAsync("JoinGameSession", playerDashboard.Id.ToString());
                        }
                    }
                    catch
                    {
                        // SignalR connection failed, but player creation succeeded
                        // Continue without real-time updates
                    }
                }
                else
                {
                    errorMessage = "Failed to create your character. Please try again.";
                }
            }
            else
            {
                var errorContent = await response.Content.ReadAsStringAsync();
                errorMessage = $"Failed to create player: {response.StatusCode}";
            }
        }
        catch (HttpRequestException ex)
        {
            errorMessage = "Cannot connect to the game server. Please make sure the game is running.";
            Logger.LogWarning(ex, "Failed to connect to game server while creating player");
        }
        catch (Exception ex)
        {
            errorMessage = "An unexpected error occurred. Please try again.";
            Logger.LogError(ex, "Unexpected error while creating player");
        }
        finally
        {
            isLoading = false;
            StateHasChanged();
        }
    }

    private async Task CreatePlayerWithCharacter()
    {
        // Clear previous errors
        errorMessage = "";
        
        // Validate character persona is selected
        if (selectedCharacterPersona == null)
        {
            errorMessage = "Please select a character to start the game.";
            Navigation.NavigateTo("/character-select");
            return;
        }

        isLoading = true;
        try
        {
            var httpClient = HttpClientFactory.CreateClient("GameAPI");
            
            // Generate unique username to avoid conflicts with duplicate character names
            var uniqueUsername = $"{selectedCharacterPersona.Name}_{DateTime.UtcNow.Ticks % 10000}";
            var request = new CreatePlayerRequest(uniqueUsername, selectedCharacterPersona.Id);
            
            var response = await httpClient.PostAsJsonAsync("api/Game/players", request);
            if (response.IsSuccessStatusCode)
            {
                playerDashboard = await response.Content.ReadFromJsonAsync<PlayerDashboardDto>();
                if (playerDashboard != null)
                {
                    gameMessage = $"Welcome, {selectedCharacterPersona.Name}! Your leadership journey begins now! 🎉";
                    try
                    {
                        if (hubConnection != null)
                        {
                            await hubConnection.SendAsync("JoinGameSession", playerDashboard.Id.ToString());
                        }
                    }
                    catch
                    {
                        // SignalR connection failed, but player creation succeeded
                        // Continue without real-time updates
                    }
                }
                else
                {
                    errorMessage = "Failed to create your character. Please try again.";
                }
            }
            else
            {
                var errorContent = await response.Content.ReadAsStringAsync();
                errorMessage = $"Failed to create player: {response.StatusCode}";
            }
        }
        catch (HttpRequestException ex)
        {
            errorMessage = "Cannot connect to the game server. Please make sure the game is running.";
            Logger.LogWarning(ex, "Failed to connect to game server while creating player");
        }
        catch (Exception ex)
        {
            errorMessage = "An unexpected error occurred. Please try again.";
            Logger.LogError(ex, "Unexpected error while creating player");
        }
        finally
        {
            isLoading = false;
            StateHasChanged();
        }
    }

    private async Task Logout()
    {
        try
        {
            await AuthService.LogoutAsync();
            currentUser = null;
            isAuthenticated = false;
            playerDashboard = null;
            gameMessage = "You have been logged out. Thanks for playing! 👋";
            StateHasChanged();
        }
        catch (Exception ex)
        {
            Logger.LogError(ex, "Error during logout");
        }
    }

    private void GoToLogin()
    {
        Navigation.NavigateTo("/login");
    }

    private async Task RollForNewJob()
    {
        if (playerDashboard == null) return;

        try
        {
            var httpClient = HttpClientFactory.CreateClient("GameAPI");
            var response = await httpClient.PostAsync($"api/game/players/{playerDashboard.Id}/roll-job", null);
            
            if (response.IsSuccessStatusCode)
            {
                var update = await response.Content.ReadFromJsonAsync<GameStateUpdate>();
                if (update != null)
                {
                    gameMessage = update.Message;
                    // In a real implementation, we would update the dashboard from the server
                }
            }
        }
        catch (Exception ex)
        {
            gameMessage = "Failed to roll for new job. Please try again.";
            Logger.LogError(ex, "Error rolling for new job for player {PlayerId}", playerDashboard.Id);
        }
    }

    private async Task LoadExistingPlayerAsync()
    {
        try
        {
            // For now, we'll always create a new player since we don't have a way to persist player data per character
            // In a full implementation, this would check localStorage or user account for existing player data
            playerDashboard = null;
        }
        catch (Exception ex)
        {
            Logger.LogError(ex, "Error checking for existing player");
            playerDashboard = null;
        }
    }

    private async Task LoadSelectedCharacterAsync()
    {
        try
        {
            // First, try to load from query parameter
            var uri = Navigation.ToAbsoluteUri(Navigation.Uri);
            var query = HttpUtility.ParseQueryString(uri.Query);
            var characterIdParam = query["character"];
            
            if (!string.IsNullOrEmpty(characterIdParam) && Guid.TryParse(characterIdParam, out var characterId))
            {
                Logger.LogInformation("Loading character from query parameter: {CharacterId}", characterId);
                
                // Load character from service
                var availablePersonas = await PersonaService.GetAvailablePersonasAsync();
                selectedCharacterPersona = availablePersonas.FirstOrDefault(p => p.Id == characterId);
                
                if (selectedCharacterPersona != null)
                {
                    // Save to localStorage for future visits
                    await LocalStorage.SetItemAsync("selectedCharacterPersona", selectedCharacterPersona);
                    Logger.LogInformation("Character loaded from query parameter and saved to localStorage: {CharacterName}", selectedCharacterPersona.Name);
                    return;
                }
                else
                {
                    Logger.LogWarning("Character with ID {CharacterId} not found in available personas", characterId);
                }
            }
            
            // Fallback: try to load from localStorage
            selectedCharacterPersona = await LocalStorage.GetItemAsync<CharacterPersona>("selectedCharacterPersona");
            
            if (selectedCharacterPersona != null)
            {
                Logger.LogInformation("Character loaded from localStorage: {CharacterName}", selectedCharacterPersona.Name);
            }
            else
            {
                Logger.LogInformation("No character found in localStorage or query parameters");
            }
        }
        catch (Exception ex)
        {
            Logger.LogError(ex, "Error loading selected character");
            selectedCharacterPersona = null;
        }
    }

    private async Task LoadGameEvents()
    {
        try
        {
            var httpClient = HttpClientFactory.CreateClient("GameAPI");
            var response = await httpClient.GetAsync("api/game/events");
            
            if (response.IsSuccessStatusCode)
            {
                var events = await response.Content.ReadFromJsonAsync<List<GameEventDto>>();
                if (events != null)
                {
                    gameEvents = events;
                }
            }
        }
        catch (Exception ex)
        {
            // Handle silently for now - events are optional
            Logger.LogWarning(ex, "Failed to load game events");
        }
    }

    private string GetEventCardClass(bool isPositive)
    {
        return isPositive ? "bg-green-50 border-l-4 border-green-400" : "bg-red-50 border-l-4 border-red-400";
    }

    private string GetEffectClass(int effect)
    {
        return effect > 0 ? "text-green-600 font-medium" : "text-red-600 font-medium";
    }

    private string FormatEffect(int effect)
    {
        return effect > 0 ? $"+{effect}" : effect.ToString();
    }

    private void OnDiceRolled(DiceRollResult result)
    {
        if (playerDashboard == null) return;

        try
        {
            Logger.LogInformation("Processing dice roll result for player {PlayerId}: Dice={Dice}, IncomeChange={IncomeChange}, ReputationChange={ReputationChange}, HappinessChange={HappinessChange}, NewJob={NewJob}", 
                playerDashboard.Id, result.DiceValue, result.IncomeChange, result.ReputationChange, result.HappinessChange, result.NewJob);

            // Update player dashboard with new values
            var newDashboard = playerDashboard with
            {
                Income = playerDashboard.Income + result.IncomeChange, // ADD the income change to current income
                Reputation = Math.Clamp(playerDashboard.Reputation + result.ReputationChange, 0, 100),
                Happiness = Math.Clamp(playerDashboard.Happiness + result.HappinessChange, 0, 100),
                CurrentJob = result.NewJob,
                LastActiveAt = DateTime.UtcNow
            };

            Logger.LogInformation("Updated player stats: Income {OldIncome} -> {NewIncome}, Reputation {OldReputation} -> {NewReputation}, Happiness {OldHappiness} -> {NewHappiness}", 
                playerDashboard.Income, newDashboard.Income, 
                playerDashboard.Reputation, newDashboard.Reputation,
                playerDashboard.Happiness, newDashboard.Happiness);

            playerDashboard = newDashboard;

            // Add to recent changes for educational feedback
            var changeRecord = new ResourceChangeRecord(
                result.IncomeChange, // This is already the change amount
                result.ReputationChange,
                result.HappinessChange,
                result.EncouragingMessage,
                EventType.Career,
                DateTime.UtcNow
            );

            recentResourceChanges.Insert(0, changeRecord);
            if (recentResourceChanges.Count > 10)
            {
                recentResourceChanges.RemoveAt(10);
            }

            gameMessage = result.EncouragingMessage;
            StateHasChanged();
            
            Logger.LogInformation("Dice roll processing completed. TerritoryDisplayer should now reload with new reputation: {Reputation} and income: {Income}", 
                newDashboard.Reputation, newDashboard.Income);
            
            // Debug: Force another StateHasChanged after a short delay to ensure UI updates
            Logger.LogInformation("Forcing additional StateHasChanged to ensure TerritoryDisplayer receives new parameters");
            StateHasChanged();
        }
        catch (Exception ex)
        {
            gameMessage = "Something went wrong with the dice roll. Try again!";
            Logger.LogError(ex, "Error handling dice roll result for player {PlayerId}", playerDashboard?.Id);
            StateHasChanged();
        }
    }

    private async Task OnTerritoryPurchased(TerritoryDto territory)
    {
        try
        {
            // Refresh player dashboard to reflect territory purchase
            if (playerDashboard != null)
            {
                playerDashboard = await LoadPlayerDashboard(playerDashboard.Id);
                gameMessage = $"🎉 Congratulations! You've acquired {territory.CountryName}! Learn about its culture and language.";
                StateHasChanged();
            }
        }
        catch (Exception ex)
        {
            gameMessage = "Territory purchase completed! Your dashboard will update shortly.";
            Logger.LogError(ex, "Error refreshing dashboard after territory purchase");
            StateHasChanged();
        }
    }

    private async Task<PlayerDashboardDto?> LoadPlayerDashboard(Guid playerId)
    {
        try
        {
            var httpClient = HttpClientFactory.CreateClient("GameAPI");
            var response = await httpClient.GetAsync($"api/Game/players/{playerId}/dashboard");
            
            if (response.IsSuccessStatusCode)
            {
                return await response.Content.ReadFromJsonAsync<PlayerDashboardDto>();
            }
        }
        catch (Exception ex)
        {
            Logger.LogError(ex, "Error loading player dashboard for player {PlayerId}", playerId);
        }
        
        return null;
    }

    private void ChangeCharacter()
    {
        // Navigate back to character selection
        Navigation.NavigateTo("/character-select");
    }

    private async Task RollDice()
    {
        try
        {
            if (playerDashboard == null) return;

            var httpClient = HttpClientFactory.CreateClient("GameAPI");
            var response = await httpClient.PostAsync($"api/Game/players/{playerDashboard.Id}/roll-dice", null);
            
            if (response.IsSuccessStatusCode)
            {
                var result = await response.Content.ReadFromJsonAsync<DiceRollResult>();
                if (result != null)
                {
                    OnDiceRolled(result);
                }
            }
            else
            {
                gameMessage = "Dice roll failed. Please try again!";
                StateHasChanged();
            }
        }
        catch (Exception ex)
        {
            gameMessage = "Something went wrong with the dice roll. Try again!";
            Logger.LogError(ex, "Error rolling dice for player {PlayerId}", playerDashboard?.Id);
            StateHasChanged();
        }
    }

    private void ViewTerritories()
    {
        // Already shown on the dashboard - could add some animation or highlight
        gameMessage = "🌍 Explore the territories below! Click on any territory to learn more about it.";
        StateHasChanged();
    }

    private void CheckLanguages()
    {
        showLanguageChallenge = true;
        StateHasChanged();
    }

    public async ValueTask DisposeAsync()
    {
        if (hubConnection != null)
        {
            await hubConnection.DisposeAsync();
        }
    }
}
